import React, { Component } from 'react'

export default class DynamicLoadingCom extends Component{
    render(){
        return(
            <div>
                使用React.lazy动态加载组件
            </div>
        )
    }
}